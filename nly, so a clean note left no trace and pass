[1mdiff --git a/app/api/generate-note/route.ts b/app/api/generate-note/route.ts[m
[1mindex 28104e5..82b893c 100644[m
[1m--- a/app/api/generate-note/route.ts[m
[1m+++ b/app/api/generate-note/route.ts[m
[36m@@ -88,11 +88,15 @@[m [mexport async function POST(req: NextRequest) {[m
     const userId: string = authedUser.id;[m
 [m
     const encoder = new TextEncoder();[m
[31m-    // Whether anything reached the client before a failure. A note that died before its first token[m
[31m-    // and one that died mid-paragraph are different incidents (the second usually means the model or[m
[31m-    // a gate failed partway, the first that setup did), and the failure alert below cannot tell them[m
[31m-    // apart after the fact — so it is recorded as it happens.[m
[32m+[m[32m    // Whether real note prose reached the client before a failure. A note that died before its first[m
[32m+[m[32m    // token and one that died mid-paragraph are different incidents (the second usually means the[m
[32m+[m[32m    // model or a gate failed partway, the first that setup did), and the failure alert below cannot[m
[32m+[m[32m    // tell them apart after the fact — so it is recorded as it happens.[m
     let emittedContent = false;[m
[32m+[m[32m    // Whether the combined compliance gate fired its one regeneration. generateSmartNote signals it[m
[32m+[m[32m    // through the SAME onChunk callback as prose, so the two must be counted separately: a note that[m
[32m+[m[32m    // regenerated and then died before writing any prose had emitted nothing the RBT could see.[m
[32m+[m[32m    let regenFired = false;[m
     const readable = new ReadableStream({[m
       async start(controller) {[m
         try {[m
[36m@@ -101,7 +105,14 @@[m [mexport async function POST(req: NextRequest) {[m
           // so we ALSO send the FILTERED final text in __META__ — clients patch the displayed note[m
           // at completion, so nothing unfiltered reaches the fill even though the live stream is raw.[m
           const result = await generateSmartNote(input, userId, (text) => {[m
[31m-            emittedContent = true;[m
[32m+[m[32m            // __REGEN__ is a control marker, not note text. Stripping it (with its framing newlines)[m
[32m+[m[32m            // leaves exactly the prose this chunk carried: if anything is left the RBT saw output, and[m
[32m+[m[32m            // if the strip changed the chunk at all the regeneration fired. generateSmartNote sends the[m
[32m+[m[32m            // marker as its own chunk today; handling a mixed chunk keeps that from becoming a[m
[32m+[m[32m            // correctness dependency.[m
[32m+[m[32m            const prose = text.replace(/\n?__REGEN__\n?/g, '');[m
[32m+[m[32m            if (prose !== text) regenFired = true;[m
[32m+[m[32m            if (prose.length > 0) emittedContent = true;[m
             controller.enqueue(encoder.encode(text));[m
           });[m
           controller.enqueue(encoder.encode([m
[36m@@ -118,6 +129,7 @@[m [mexport async function POST(req: NextRequest) {[m
             clientId: input.clientId,[m
             userId,[m
             emittedContent,[m
[32m+[m[32m            regenFired,[m
             error: e?.message || String(e),[m
           }, e);[m
           // Started BEFORE the enqueue so a client that has already disconnected (which makes[m
[36m@@ -137,6 +149,8 @@[m [mexport async function POST(req: NextRequest) {[m
               stack: e?.stack || null,[m
               // True = the RBT watched a partial note appear and then fail.[m
               emittedContent,[m
[32m+[m[32m              // True = the gate had already spent its one regeneration before the failure.[m
[32m+[m[32m              regenFired,[m
             },[m
           });[m
           // BLOCKING: the note must not be used (a compliance hard-stop, or generation failed[m
[1mdiff --git a/lib/adminAlerts.ts b/lib/adminAlerts.ts[m
[1mindex 2ce35ce..c37112b 100644[m
[1m--- a/lib/adminAlerts.ts[m
[1m+++ b/lib/adminAlerts.ts[m
[36m@@ -34,10 +34,21 @@[m [mexport type AlertSeverity = 'critical' | 'warning' | 'info'[m
 [m
 // The event vocabulary. Dotted keys, `<area>.<event>`, defined HERE rather than as free strings at[m
 // call sites so the set stays enumerable — the admin panel groups on it, and a typo at a call site[m
[31m-// would silently create a category nothing ever reads. Later commits extend this union; today the[m
[31m-// note-generation hard-failure path is the only producer.[m
[32m+[m[32m// would silently create a category nothing ever reads. Later commits extend this union.[m
 export type AlertType =[m
[32m+[m[32m  // A note that never reached the RBT. Severity 'critical'.[m
   | 'note.generation_failed'[m
[32m+[m[32m  // Every note that DID reach the RBT — exactly one row per note, carrying the whole outcome[m
[32m+[m[32m  // (tiers, gate result, regeneration, flag counts) in its payload. This is what makes pass rates[m
[32m+[m[32m  // and regeneration volume computable: gate_findings is defect-only and records nothing for a[m
[32m+[m[32m  // clean note, so without this event a successful generation leaves no trace at all.[m
[32m+[m[32m  // Severity 'info' ALWAYS — including when the gate found violations. A note that shipped is a[m
[32m+[m[32m  // normal outcome; the payload carries the detail, severity carries the urgency.[m
[32m+[m[32m  | 'note.generated'[m
[32m+[m[32m  // Preselection fell back to letting the model choose. The note still ships and the gates still[m
[32m+[m[32m  // enforce, but its generation_context is NULL — so this is the record of WHY a row has no[m
[32m+[m[32m  // rotation history. Severity 'warning'.[m
[32m+[m[32m  | 'note.preselect_failed'[m
 [m
 export interface AdminAlertInput {[m
   source: AlertSource[m
[1mdiff --git a/lib/complianceGate.test.mjs b/lib/complianceGate.test.mjs[m
[1mindex 8821593..a2b8e54 100644[m
[1m--- a/lib/complianceGate.test.mjs[m
[1m+++ b/lib/complianceGate.test.mjs[m
[36m@@ -12,6 +12,7 @@[m [mimport {[m
   buildComplianceRegenInstruction,[m
   runCombinedComplianceGate,[m
   interventionViolationNames,[m
[32m+[m[32m  summarizeSurvivingViolations,[m
 } from './complianceGate.ts';[m
 [m
 const clean = (overrides = {}) => ({[m
[36m@@ -162,3 +163,52 @@[m [mtest('an unsegmentable note does NOT force a coverage regen (missing is empty wh[m
   const instr = buildComplianceRegenInstruction(clean({ coverage: { segmentable: false, missing: [] } }));[m
   assert.equal(instr, null, 'unsegmentable + otherwise clean → no combined regen');[m
 });[m
[32m+[m
[32m+[m[32m// ── summarizeSurvivingViolations: the gateClean metric ───────────────────────────────────────────[m
[32m+[m[32m// `clean` is the pass rate the admin feed is computed from, so its definition is pinned here — a[m
[32m+[m[32m// silent drift in what counts as "clean" would silently move every number derived from it.[m
[32m+[m
[32m+[m[32mtest('summarize: a clean state is clean and names no violations', () => {[m
[32m+[m[32m  const { clean: isClean, violations } = summarizeSurvivingViolations(clean());[m
[32m+[m[32m  assert.equal(isClean, true);[m
[32m+[m[32m  assert.deepEqual(violations, {[m
[32m+[m[32m    prohibited: [], unapproved: [], skillAsReduction: [],[m
[32m+[m[32m    approvedFunction: [], teachingMethod: [], coverageMissing: [], unsegmentable: false,[m
[32m+[m[32m  });[m
[32m+[m[32m});[m
[32m+[m
[32m+[m[32mtest('summarize: a multi-defect state is not clean and reports every bucket', () => {[m
[32m+[m[32m  const { clean: isClean, violations } = summarizeSurvivingViolations(multiDefect());[m
[32m+[m[32m  assert.equal(isClean, false);[m
[32m+[m[32m  assert.deepEqual(violations.prohibited, ['RIRD']);[m
[32m+[m[32m  assert.deepEqual(violations.approvedFunction, ['Throwing Objects'], 'behavior name only, not the triple');[m
[32m+[m[32m  assert.deepEqual(violations.coverageMissing, ['Off-Task Behavior']);[m
[32m+[m[32m  assert.deepEqual(violations.teachingMethod, ['DTT']);[m
[32m+[m[32m  assert.equal(violations.unsegmentable, false);[m
[32m+[m[32m});[m
[32m+[m
[32m+[m[32mtest('summarize: EACH bucket alone is enough to make a note not clean', () => {[m
[32m+[m[32m  const cases = [[m
[32m+[m[32m    ['prohibited', clean({ intervention: { prohibited: ['RIRD'], unapproved: [], skillAsReduction: [] } })],[m
[32m+[m[32m    ['unapproved', clean({ intervention: { prohibited: [], unapproved: ['Guided Compliance'], skillAsReduction: [] } })],[m
[32m+[m[32m    ['skillAsReduction', clean({ intervention: { prohibited: [], unapproved: [], skillAsReduction: ['FCT'] } })],[m
[32m+[m[32m    ['approvedFunction', clean({ functionViolations: [{ name: 'Elopement', wrote: 'Automatic', approved: ['escape'] }] })],[m
[32m+[m[32m    ['teachingMethod', clean({ methodViolations: ['DTT'] })],[m
[32m+[m[32m    ['coverageMissing', clean({ coverage: { segmentable: true, missing: [{ name: 'Elopement' }] } })],[m
[32m+[m[32m  ];[m
[32m+[m[32m  for (const [label, state] of cases) {[m
[32m+[m[32m    assert.equal(summarizeSurvivingViolations(state).clean, false, `${label} alone must not be clean`);[m
[32m+[m[32m  }[m
[32m+[m[32m});[m
[32m+[m
[32m+[m[32mtest('summarize: UNSEGMENTABLE is not clean — unverifiable must never count as passed', () => {[m
[32m+[m[32m  // The pass rate stays conservative: coverage could not be checked, so the note is not known-good.[m
[32m+[m[32m  // Reported on its own key so "could not verify" stays separable from "actually defective".[m
[32m+[m[32m  const { clean: isClean, violations } = summarizeSurvivingViolations([m
[32m+[m[32m    clean({ coverage: { segmentable: false, missing: [] } }),[m
[32m+[m[32m  );[m
[32m+[m[32m  assert.equal(isClean, false, 'an unsegmentable note must not be counted as a clean pass');[m
[32m+[m[32m  assert.equal(violations.unsegmentable, true);[m
[32m+[m[32m  assert.deepEqual(violations.coverageMissing, [], 'no missing names are claimed when nothing could be checked');[m
[32m+[m[32m  assert.deepEqual(violations.prohibited, [], 'and no other bucket is fabricated');[m
[32m+[m[32m});[m
[1mdiff --git a/lib/complianceGate.ts b/lib/complianceGate.ts[m
[1mindex 4f8e3c1..89362a8 100644[m
[1m--- a/lib/complianceGate.ts[m
[1m+++ b/lib/complianceGate.ts[m
[36m@@ -45,6 +45,50 @@[m [mexport function interventionViolationNames(v: InterventionViolations): string[][m
   return [...new Set([...v.prohibited, ...v.unapproved, ...v.skillAsReduction])];[m
 }[m
 [m
[32m+[m[32m// What survived the combined gate, summarized for the note-outcome record (admin_alerts[m
[32m+[m[32m// 'note.generated'). Pure, and it lives here rather than at the emit site because `clean` IS the[m
[32m+[m[32m// pass-rate metric — if its definition drifts, every number computed from the feed drifts silently[m
[32m+[m[32m// with it. Pinned by tests in complianceGate.test.mjs.[m
[32m+[m[32m//[m
[32m+[m[32m// UNSEGMENTABLE COUNTS AS NOT CLEAN, deliberately. An unsegmentable note is not known-defective — it[m
[32m+[m[32m// is UNVERIFIABLE, because coverage could not be checked at all. Folding it into `clean` would[m
[32m+[m[32m// overstate the pass rate by counting unchecked notes as passed, so the metric stays conservative and[m
[32m+[m[32m// `unsegmentable` is reported as its own key, letting "actually defective" be separated from "could[m
[32m+[m[32m// not verify" downstream.[m
[32m+[m[32mexport interface SurvivingViolations {[m
[32m+[m[32m  prohibited: string[][m
[32m+[m[32m  unapproved: string[][m
[32m+[m[32m  skillAsReduction: string[][m
[32m+[m[32m  // Behavior names only — the full triples live in gate_findings; this is a summary, not a duplicate.[m
[32m+[m[32m  approvedFunction: string[][m
[32m+[m[32m  teachingMethod: string[][m
[32m+[m[32m  coverageMissing: string[][m
[32m+[m[32m  unsegmentable: boolean[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mexport function summarizeSurvivingViolations([m
[32m+[m[32m  state: ComplianceState,[m
[32m+[m[32m): { clean: boolean; violations: SurvivingViolations } {[m
[32m+[m[32m  const violations: SurvivingViolations = {[m
[32m+[m[32m    prohibited: state.intervention.prohibited,[m
[32m+[m[32m    unapproved: state.intervention.unapproved,[m
[32m+[m[32m    skillAsReduction: state.intervention.skillAsReduction,[m
[32m+[m[32m    approvedFunction: state.functionViolations.map((v) => v.name),[m
[32m+[m[32m    teachingMethod: state.methodViolations,[m
[32m+[m[32m    coverageMissing: state.coverage.segmentable ? state.coverage.missing.map((m) => m.name) : [],[m
[32m+[m[32m    unsegmentable: !state.coverage.segmentable,[m
[32m+[m[32m  }[m
[32m+[m[32m  const clean =[m
[32m+[m[32m    !violations.prohibited.length &&[m
[32m+[m[32m    !violations.unapproved.length &&[m
[32m+[m[32m    !violations.skillAsReduction.length &&[m
[32m+[m[32m    !violations.approvedFunction.length &&[m
[32m+[m[32m    !violations.teachingMethod.length &&[m
[32m+[m[32m    !violations.coverageMissing.length &&[m
[32m+[m[32m    !violations.unsegmentable[m
[32m+[m[32m  return { clean, violations }[m
[32m+[m[32m}[m
[32m+[m
 // Build ONE combined regeneration instruction from ONLY the checks that failed, or null when the note is[m
 // clean. Order: function coverage + approved function first (both about the function — name it, and from the[m
 // approved set — so they reinforce rather than conflict), then interventions, then teaching methods. Each[m
[1mdiff --git a/lib/generateSmartNote.ts b/lib/generateSmartNote.ts[m
[1mindex 616598f..cb4c67d 100644[m
[1m--- a/lib/generateSmartNote.ts[m
[1m+++ b/lib/generateSmartNote.ts[m
[36m@@ -20,13 +20,14 @@[m [mimport { stripInvalidNextSession } from '@/lib/nextSessionDate';[m
 import { findRedFlagFlags } from '@/lib/redFlagPhrases';[m
 import { decideUniqueness } from '@/lib/noteSimilarity';[m
 import {[m
[31m-  runCombinedComplianceGate, type ComplianceState,[m
[32m+[m[32m  runCombinedComplianceGate, summarizeSurvivingViolations, type ComplianceState,[m
 } from '@/lib/complianceGate';[m
 import { buildInterventionDetail } from '@/lib/interventionDetail';[m
 import { preselect, buildFixedAssignmentsBlock, type PreselectResult } from '@/lib/preselect';[m
 import { readGenerationHistory } from '@/lib/rotationHistory';[m
[31m-import { assignTiers } from '@/lib/complianceTiers';[m
[32m+[m[32mimport { assignTiers, tierCounts } from '@/lib/complianceTiers';[m
 import { collectGateFindings, recordGateFindings } from '@/lib/gateFindings';[m
[32m+[m[32mimport { emitAdminAlert } from '@/lib/adminAlerts';[m
 [m
 const openai = new OpenAI({[m
   apiKey: process.env.AZURE_OPENAI_API_KEY || 'azure-openai',[m
[36m@@ -279,10 +280,18 @@[m [mexport async function generateSmartNote(input: SessionInput, rbtId?: string, onC[m
       resolvedProfile.approvedInterventions[m
     );[m
   }[m
[32m+[m[32m  // How much the location filters removed. Both drops are SILENT to the RBT — an activity or skill[m
[32m+[m[32m  // that does not fit the session's location simply disappears from the note — so the counts are[m
[32m+[m[32m  // captured here and reported on the outcome record. A client whose skills are consistently dropped[m
[32m+[m[32m  // is a setting/profile mismatch, invisible until now.[m
[32m+[m[32m  let activitiesDropped = 0;[m
[32m+[m[32m  let skillsDropped = 0;[m
   if (input.activitiesUsed?.length) {[m
[32m+[m[32m    const beforeActivities = input.activitiesUsed.length;[m
     input.activitiesUsed = input.activitiesUsed.filter(a =>[m
       isValidActivity(a.name, input.sessionInfo.location)[m
     );[m
[32m+[m[32m    activitiesDropped = beforeActivities - input.activitiesUsed.length;[m
   }[m
   if (input.behaviorsObserved?.length) {[m
     input.behaviorsObserved = input.behaviorsObserved.map(b => ({[m
[36m@@ -291,10 +300,12 @@[m [mexport async function generateSmartNote(input: SessionInput, rbtId?: string, onC[m
     }));[m
   }[m
   if (resolvedProfile.activePrograms?.replacementSkills?.length) {[m
[32m+[m[32m    const beforeSkills = resolvedProfile.activePrograms.replacementSkills.length;[m
     resolvedProfile.activePrograms.replacementSkills =[m
       resolvedProfile.activePrograms.replacementSkills.filter((s: string) =>[m
         isValidSkillForLocation(s, input.sessionInfo.location)[m
       );[m
[32m+[m[32m    skillsDropped = beforeSkills - resolvedProfile.activePrograms.replacementSkills.length;[m
   }[m
 [m
   // Steps 2, 3, 5: Run all DB queries in parallel[m
[36m@@ -412,12 +423,16 @@[m [mexport async function generateSmartNote(input: SessionInput, rbtId?: string, onC[m
   const activityNames = (input.activitiesUsed ?? []).map((a) => a.name).filter(Boolean);[m
   let generationContext: PreselectResult | null = null;[m
   let fixedAssignmentsBlock = '';[m
[32m+[m[32m  // Compliance controller: deterministic outcome tier per ABC and per skill from the RBT's level. Below[m
[32m+[m[32m  // typical / poor never make every item fail; typical never all-perfect — guaranteed by assignTiers.[m
[32m+[m[32m  //[m
[32m+[m[32m  // Computed OUTSIDE the try below (it used to sit inside). assignTiers is pure arithmetic over a length[m
[32m+[m[32m  // — no I/O, nothing that can throw — so this cannot change which path runs, and it means the tier[m
[32m+[m[32m  // distribution is still reportable on the outcome record when the preselect I/O fails.[m
[32m+[m[32m  const behaviorTiers = assignTiers(input.complianceLevel, input.behaviorsObserved.length);[m
[32m+[m[32m  const skillTiers = assignTiers(input.complianceLevel, input.replacementSkillsAddressed.length);[m
   try {[m
     const history = await readGenerationHistory(prisma, input.clientId, { window: 3 });[m
[31m-    // Compliance controller: deterministic outcome tier per ABC and per skill from the RBT's level. Below[m
[31m-    // typical / poor never make every item fail; typical never all-perfect — guaranteed by assignTiers.[m
[31m-    const behaviorTiers = assignTiers(input.complianceLevel, input.behaviorsObserved.length);[m
[31m-    const skillTiers = assignTiers(input.complianceLevel, input.replacementSkillsAddressed.length);[m
     generationContext = preselect({[m
       behaviors: input.behaviorsObserved.map((b) => ({[m
         name: b.name,[m
[36m@@ -436,11 +451,30 @@[m [mexport async function generateSmartNote(input: SessionInput, rbtId?: string, onC[m
       history,[m
     });[m
     fixedAssignmentsBlock = buildFixedAssignmentsBlock(generationContext);[m
[31m-  } catch {[m
[32m+[m[32m  } catch (e: any) {[m
     // Preselection is best-effort: if history read or selection fails, fall back to the pre-preselection[m
     // path (GPT chooses, the gates still enforce). A rotation hiccup must never cost a note.[m
[32m+[m[32m    // FALLBACK BEHAVIOR IS UNCHANGED — the two assignments below are exactly what this catch always did.[m
     generationContext = null;[m
     fixedAssignmentsBlock = '';[m
[32m+[m[32m    // The error used to be discarded by a bare `catch {}`, which is why notes exist with a NULL[m
[32m+[m[32m    // generation_context and no record of why. Observability only: the alert never alters the fallback.[m
[32m+[m[32m    await emitAdminAlert({[m
[32m+[m[32m      source: 'note',[m
[32m+[m[32m      type: 'note.preselect_failed',[m
[32m+[m[32m      severity: 'warning',[m
[32m+[m[32m      actorUserId: rbtId,[m
[32m+[m[32m      clientId: input.clientId,[m
[32m+[m[32m      payload: {[m
[32m+[m[32m        message: e?.message || String(e),[m
[32m+[m[32m        name: e?.name || null,[m
[32m+[m[32m        stack: e?.stack || null,[m
[32m+[m[32m        // The two locked sets preselection needs. Empty ones are the likeliest cause of a selection[m
[32m+[m[32m        // failure (as opposed to the history read failing), so they separate a config gap from a DB blip.[m
[32m+[m[32m        approvedInterventionsEmpty: !(resolvedProfile.approvedInterventions ?? []).length,[m
[32m+[m[32m        approvedMethodSetEmpty: !approvedMethodSet.length,[m
[32m+[m[32m      },[m
[32m+[m[32m    });[m
   }[m
   // The ABC count IS the number of behaviors the RBT documented — never a fixed target. A fixed[m
   // "exactly 5" forced the model to invent ABCs for behaviors the RBT never marked, sourcing them[m
[36m@@ -719,6 +753,51 @@[m [mexport async function generateSmartNote(input: SessionInput, rbtId?: string, onC[m
   // /api/extension/save-note from the extension), each of which already guards against exact duplicates.[m
   // A regeneration therefore never adds a row and never silently replaces a note the RBT already saved.[m
 [m
[32m+[m[32m  // Step 8b: THE OUTCOME RECORD. Exactly ONE row per note that reached the RBT, carrying the whole[m
[32m+[m[32m  // result in its payload — deliberately not one event per signal. This fires on every single note, so[m
[32m+[m[32m  // volume is the design constraint: N rows per note would make the feed unreadable and the table grow[m
[32m+[m[32m  // N times faster for no extra information.[m
[32m+[m[32m  //[m
[32m+[m[32m  // This is what makes pass rates and regeneration volume computable. gate_findings answers "which gate[m
[32m+[m[32m  // fired" and is defect-only — it records NOTHING for a clean note — so the denominator of any rate[m
[32m+[m[32m  // (how many notes were generated at all) did not exist anywhere until this event. The two coexist:[m
[32m+[m[32m  // gate_findings keeps the per-finding clinical detail, this keeps the per-note outcome.[m
[32m+[m[32m  //[m
[32m+[m[32m  // Severity is ALWAYS 'info', including when gateClean is false: a note that shipped after its one[m
[32m+[m[32m  // regeneration is a normal outcome, not an incident. The payload carries the detail; severity carries[m
[32m+[m[32m  // the urgency, and inflating it here would drown the criticals that mean a user lost their note.[m
[32m+[m[32m  const { clean: gateClean, violations: survivingViolations } = summarizeSurvivingViolations(gate.state);[m
[32m+[m[32m  await emitAdminAlert({[m
[32m+[m[32m    source: 'note',[m
[32m+[m[32m    type: 'note.generated',[m
[32m+[m[32m    severity: 'info',[m
[32m+[m[32m    actorUserId: rbtId,[m
[32m+[m[32m    clientId: input.clientId,[m
[32m+[m[32m    payload: {[m
[32m+[m[32m      complianceLevel: input.complianceLevel ?? null,[m
[32m+[m[32m      // Counts per tier, not the full arrays — the distribution is what a rate is computed from, and[m
[32m+[m[32m      // the per-item ordering is already reproducible from complianceLevel + the item counts.[m
[32m+[m[32m      behaviorTiers: tierCounts(behaviorTiers),[m
[32m+[m[32m      skillTiers: tierCounts(skillTiers),[m
[32m+[m[32m      behaviorCount: input.behaviorsObserved.length,[m
[32m+[m[32m      skillCount: input.replacementSkillsAddressed.length,[m
[32m+[m[32m      regenCount: gate.regenCount,[m
[32m+[m[32m      gateClean,[m
[32m+[m[32m      // Present ONLY when something survived, so a clean note's payload stays small and "has the key"[m
[32m+[m[32m      // is itself the signal. See summarizeSurvivingViolations for why unsegmentable is not clean.[m
[32m+[m[32m      ...(gateClean ? {} : { survivingViolations }),[m
[32m+[m[32m      similarityWarning,[m
[32m+[m[32m      coherenceFlagCount: coherenceFlags.length,[m
[32m+[m[32m      redFlagCount: redFlags.length,[m
[32m+[m[32m      // Silent location-filter drops, captured at the top of this function.[m
[32m+[m[32m      activitiesDropped,[m
[32m+[m[32m      skillsDropped,[m
[32m+[m[32m      // false = this note will save with a NULL generation_context (preselection fell back); the[m
[32m+[m[32m      // matching 'note.preselect_failed' alert carries the reason.[m
[32m+[m[32m      hasGenerationContext: generationContext !== null,[m
[32m+[m[32m    },[m
[32m+[m[32m  });[m
[32m+[m
   return {[m
     note,[m
     clientId: input.clientId,[m
[1mdiff --git a/scripts/verify-admin-alerts.test.mjs b/scripts/verify-admin-alerts.test.mjs[m
[1mindex 449f1d6..089571f 100644[m
[1m--- a/scripts/verify-admin-alerts.test.mjs[m
[1m+++ b/scripts/verify-admin-alerts.test.mjs[m
[36m@@ -1,15 +1,26 @@[m
 // ─────────────────────────────────────────────────────────────────────────────[m
 // ADMIN-ALERTS END-TO-END VERIFICATION[m
 //[m
[31m-// Proves the Commit 1 emitter actually reaches the database: calls the REAL emitAdminAlert from[m
[31m-// lib/adminAlerts.ts, then reads the row back over a SEPARATE raw `pg` connection — deliberately NOT[m
[32m+[m[32m// Proves the emitter actually reaches the database: calls the REAL emitAdminAlert from[m
[32m+[m[32m// lib/adminAlerts.ts, then reads the rows back over a SEPARATE raw `pg` connection — deliberately NOT[m
 // through Prisma, so the write and the read do not share the layer under test. A round-trip that only[m
 // used Prisma for both halves would still pass if the delegate wrote to the wrong table or silently[m
 // coerced a field.[m
 //[m
[31m-// WRITES TO THE LIVE DATABASE, then removes exactly what it wrote. The row is scoped by a[m
[31m-// per-run `ts` in its payload and deleted BY ID in a finally block, so a failed assertion still[m
[31m-// cleans up. It touches no other row and no application code.[m
[32m+[m[32m// ── WHAT THIS DOES AND DOES NOT PROVE ───────────────────────────────────────[m
[32m+[m[32m// It proves the ALERT PATH: that each alert type round-trips, that one emit produces exactly one row,[m
[32m+[m[32m// and — the new risk in commit 2 — that the rich 'note.generated' outcome payload survives JSONB[m
[32m+[m[32m// intact, nested objects and all, including the conditional key that must be ABSENT on a clean note.[m
[32m+[m[32m//[m
[32m+[m[32m// It does NOT prove that generateSmartNote emits 'note.generated' exactly once per note. That is a[m
[32m+[m[32m// property of the generation pipeline, not of the emitter, and asserting it honestly requires a LIVE[m
[32m+[m[32m// generation: a real Azure OpenAI call against a real client's clinical profile. That is not[m
[32m+[m[32m// automated here rather than faked with a stub that would prove nothing. To check it by hand, generate[m
[32m+[m[32m// one note and then run the query printed at the end of a passing run.[m
[32m+[m[32m//[m
[32m+[m[32m// WRITES TO THE LIVE DATABASE, then removes exactly what it wrote. Every row carries this run's id in[m
[32m+[m[32m// its payload and the cleanup sweep is scoped to that id, so a failed assertion still cleans up and[m
[32m+[m[32m// nothing else can ever match. It touches no other row and no application code.[m
 //[m
 // RUN:  node --env-file=.env.local scripts/verify-admin-alerts.test.mjs[m
 //[m
[36m@@ -17,8 +28,9 @@[m
 // glob is lib/**/*.test.mjs — it must stay opt-in, because unlike the unit suite it needs a database[m
 // and performs a write.[m
 // ─────────────────────────────────────────────────────────────────────────────[m
[31m-import { register } from 'node:module';[m
[31m-import { createRequire } from 'node:module';[m
[32m+[m[32mimport { register, createRequire } from 'node:module';[m
[32m+[m[32mimport { randomUUID } from 'node:crypto';[m
[32m+[m[32mimport { deepStrictEqual } from 'node:assert/strict';[m
 [m
 const ROOT = new URL('../', import.meta.url).href; // scripts/ -> project root[m
 [m
[36m@@ -61,10 +73,40 @@[m [mconst { Pool } = require('pg');[m
 [m
 // ── Result reporting ─────────────────────────────────────────────────────────[m
 const failures = [];[m
[31m-function check(label, actual, expected) {[m
[31m-  const ok = Object.is(actual, expected);[m
[31m-  if (!ok) failures.push(`${label}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);[m
[31m-  return ok;[m
[32m+[m[32mfunction eq(label, actual, expected) {[m
[32m+[m[32m  if (!Object.is(actual, expected)) {[m
[32m+[m[32m    failures.push(`${label}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m[32m// Structural comparison for values that made a round-trip through JSONB.[m
[32m+[m[32m//[m
[32m+[m[32m// MUST NOT be JSON.stringify-based. Postgres jsonb does not store key insertion order — it normalizes[m
[32m+[m[32m// object keys (by length, then bytewise), so {FAVORABLE,PARTIAL,DIFFICULT} comes back as[m
[32m+[m[32m// {PARTIAL,DIFFICULT,FAVORABLE}. That is documented, correct jsonb behavior, and switching the column[m
[32m+[m[32m// to `json` to preserve order would be worse in every other respect (no indexing, no operators, bigger[m
[32m+[m[32m// rows). A string comparison therefore reports a false mismatch on identical data.[m
[32m+[m[32m//[m
[32m+[m[32m// node:assert deepStrictEqual has exactly the semantics needed, verified against this case:[m
[32m+[m[32m// object key ORDER is ignored while the key SET is strict (a missing, extra, or null-vs-absent key[m
[32m+[m[32m// still fails), and ARRAY ORDER remains strict — order is meaningful for things like[m
[32m+[m[32m// prohibited: ['RIRD'], so ['RIRD','FCT'] must never equal ['FCT','RIRD'].[m
[32m+[m[32mfunction deepEq(label, actual, expected) {[m
[32m+[m[32m  try {[m
[32m+[m[32m    deepStrictEqual(actual, expected);[m
[32m+[m[32m  } catch {[m
[32m+[m[32m    // Sorted-key rendering, so a reported mismatch is always about content and can never be[m
[32m+[m[32m    // misread as the ordering artifact this function exists to ignore.[m
[32m+[m[32m    failures.push(`${label}: expected ${stable(expected)}, got ${stable(actual)}`);[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m// JSON with object keys sorted at every depth. Diagnostics only — never used for comparison.[m
[32m+[m[32mfunction stable(v) {[m
[32m+[m[32m  return JSON.stringify(v, (_k, val) =>[m
[32m+[m[32m    val && typeof val === 'object' && !Array.isArray(val)[m
[32m+[m[32m      ? Object.fromEntries(Object.keys(val).sort().map((k) => [k, val[k]]))[m
[32m+[m[32m      : val,[m
[32m+[m[32m  );[m
 }[m
 function fail(reason) {[m
   console.log(`\nFAIL — ${reason}`);[m
[36m@@ -79,17 +121,147 @@[m [mif (!process.env.DATABASE_URL) {[m
 // exactly the way the application does — if the app can reach the database, so can this.[m
 const pool = new Pool({ connectionString: process.env.DATABASE_URL });[m
 [m
[31m-// Unique per run; used to find the row again and to scope the cleanup so nothing else can match.[m
[31m-const ts = new Date().toISOString();[m
[32m+[m[32m// Scopes every row this run writes, and scopes the cleanup sweep that removes them.[m
[32m+[m[32mconst runId = randomUUID();[m
 [m
[31m-let insertedId = null;[m
[32m+[m[32m// ── The cases ────────────────────────────────────────────────────────────────[m
[32m+[m[32m// Representative payloads matching what the application actually emits, so the round-trip exercises[m
[32m+[m[32m// the real shapes rather than a toy object.[m
[32m+[m[32mconst CLEAN_OUTCOME = {[m
[32m+[m[32m  complianceLevel: 'below_typical',[m
[32m+[m[32m  behaviorTiers: { FAVORABLE: 1, PARTIAL: 2, DIFFICULT: 0 },[m
[32m+[m[32m  skillTiers: { FAVORABLE: 1, PARTIAL: 1, DIFFICULT: 0 },[m
[32m+[m[32m  behaviorCount: 3,[m
[32m+[m[32m  skillCount: 2,[m
[32m+[m[32m  regenCount: 0,[m
[32m+[m[32m  gateClean: true,[m
[32m+[m[32m  similarityWarning: false,[m
[32m+[m[32m  coherenceFlagCount: 0,[m
[32m+[m[32m  redFlagCount: 0,[m
[32m+[m[32m  activitiesDropped: 0,[m
[32m+[m[32m  skillsDropped: 0,[m
[32m+[m[32m  hasGenerationContext: true,[m
[32m+[m[32m};[m
[32m+[m
[32m+[m[32mconst SURVIVING = {[m
[32m+[m[32m  prohibited: ['RIRD'],[m
[32m+[m[32m  unapproved: [],[m
[32m+[m[32m  skillAsReduction: ['FCT'],[m
[32m+[m[32m  approvedFunction: ['Throwing Objects'],[m
[32m+[m[32m  teachingMethod: ['DTT'],[m
[32m+[m[32m  coverageMissing: [],[m
[32m+[m[32m  unsegmentable: true,[m
[32m+[m[32m};[m
[32m+[m
[32m+[m[32mconst CASES = [[m
[32m+[m[32m  {[m
[32m+[m[32m    label: 'note.generation_failed',[m
[32m+[m[32m    alert: {[m
[32m+[m[32m      source: 'note',[m
[32m+[m[32m      type: 'note.generation_failed',[m
[32m+[m[32m      severity: 'critical',[m
[32m+[m[32m      payload: {[m
[32m+[m[32m        message: 'verification synthetic failure',[m
[32m+[m[32m        name: 'Error',[m
[32m+[m[32m        stack: 'Error: verification synthetic failure\n    at verify',[m
[32m+[m[32m        emittedContent: false,[m
[32m+[m[32m        // Commit 2 item 1: the flag split. A note that regenerated then died before any prose must[m
[32m+[m[32m        // record emittedContent:false WITH regenFired:true — the pair the old single flag conflated.[m
[32m+[m[32m        regenFired: true,[m
[32m+[m[32m      },[m
[32m+[m[32m    },[m
[32m+[m[32m    expect(row, p) {[m
[32m+[m[32m      eq('severity', row.severity, 'critical');[m
[32m+[m[32m      eq('payload.emittedContent', p.emittedContent, false);[m
[32m+[m[32m      eq('payload.regenFired', p.regenFired, true);[m
[32m+[m[32m      eq('payload.name', p.name, 'Error');[m
[32m+[m[32m      if (typeof p.stack !== 'string' || !p.stack.includes('at verify')) {[m
[32m+[m[32m        failures.push('payload.stack did not survive the round-trip');[m
[32m+[m[32m      }[m
[32m+[m[32m    },[m
[32m+[m[32m  },[m
[32m+[m[32m  {[m
[32m+[m[32m    label: 'note.generated (clean)',[m
[32m+[m[32m    alert: {[m
[32m+[m[32m      source: 'note',[m
[32m+[m[32m      type: 'note.generated',[m
[32m+[m[32m      severity: 'info',[m
[32m+[m[32m      payload: CLEAN_OUTCOME,[m
[32m+[m[32m    },[m
[32m+[m[32m    expect(row, p) {[m
[32m+[m[32m      eq('severity', row.severity, 'info');[m
[32m+[m[32m      eq('type', row.type, 'note.generated');[m
[32m+[m[32m      eq('payload.gateClean', p.gateClean, true);[m
[32m+[m[32m      eq('payload.regenCount', p.regenCount, 0);[m
[32m+[m[32m      // Nested objects must survive JSONB as objects, not as strings.[m
[32m+[m[32m      deepEq('payload.behaviorTiers', p.behaviorTiers, CLEAN_OUTCOME.behaviorTiers);[m
[32m+[m[32m      deepEq('payload.skillTiers', p.skillTiers, CLEAN_OUTCOME.skillTiers);[m
[32m+[m[32m      eq('payload.complianceLevel', p.complianceLevel, 'below_typical');[m
[32m+[m[32m      eq('payload.hasGenerationContext', p.hasGenerationContext, true);[m
[32m+[m[32m      eq('payload.activitiesDropped', p.activitiesDropped, 0);[m
[32m+[m[32m      eq('payload.skillsDropped', p.skillsDropped, 0);[m
[32m+[m[32m      // The conditional key: on a clean note the key must be ABSENT, not present-and-empty. "Has the[m
[32m+[m[32m      // key" is itself the signal, so a null or {} here would be a real defect.[m
[32m+[m[32m      //[m
[32m+[m[32m      // DELIBERATELY A KEY-PRESENCE TEST, NOT deepEq. Comparing structurally against `undefined`[m
[32m+[m[32m      // would pass for a key that is present and set to null — which is exactly the case this must[m
[32m+[m[32m      // catch. hasOwnProperty rather than `in` so nothing inherited from Object.prototype can be[m
[32m+[m[32m      // mistaken for a payload key.[m
[32m+[m[32m      if (Object.prototype.hasOwnProperty.call(p, 'survivingViolations')) {[m
[32m+[m[32m        failures.push([m
[32m+[m[32m          `survivingViolations must be OMITTED entirely when gateClean is true, got ${stable(p.survivingViolations)}`,[m
[32m+[m[32m        );[m
[32m+[m[32m      }[m
[32m+[m[32m    },[m
[32m+[m[32m  },[m
[32m+[m[32m  {[m
[32m+[m[32m    label: 'note.generated (not clean)',[m
[32m+[m[32m    alert: {[m
[32m+[m[32m      source: 'note',[m
[32m+[m[32m      type: 'note.generated',[m
[32m+[m[32m      severity: 'info',[m
[32m+[m[32m      payload: { ...CLEAN_OUTCOME, regenCount: 1, gateClean: false, survivingViolations: SURVIVING },[m
[32m+[m[32m    },[m
[32m+[m[32m    expect(row, p) {[m
[32m+[m[32m      // Severity stays 'info' even when the gate found violations — a note that shipped is a normal[m
[32m+[m[32m      // outcome. If this ever reads 'warning' the feed will drown the criticals that mean a lost note.[m
[32m+[m[32m      eq('severity', row.severity, 'info');[m
[32m+[m[32m      eq('payload.gateClean', p.gateClean, false);[m
[32m+[m[32m      eq('payload.regenCount', p.regenCount, 1);[m
[32m+[m[32m      deepEq('payload.survivingViolations', p.survivingViolations, SURVIVING);[m
[32m+[m[32m      eq('payload.survivingViolations.unsegmentable', p.survivingViolations?.unsegmentable, true);[m
[32m+[m[32m    },[m
[32m+[m[32m  },[m
[32m+[m[32m  {[m
[32m+[m[32m    label: 'note.preselect_failed',[m
[32m+[m[32m    alert: {[m
[32m+[m[32m      source: 'note',[m
[32m+[m[32m      type: 'note.preselect_failed',[m
[32m+[m[32m      severity: 'warning',[m
[32m+[m[32m      payload: {[m
[32m+[m[32m        message: 'verification synthetic preselect failure',[m
[32m+[m[32m        name: 'TypeError',[m
[32m+[m[32m        stack: 'TypeError: verification synthetic preselect failure\n    at verify',[m
[32m+[m[32m        approvedInterventionsEmpty: true,[m
[32m+[m[32m        approvedMethodSetEmpty: false,[m
[32m+[m[32m      },[m
[32m+[m[32m    },[m
[32m+[m[32m    expect(row, p) {[m
[32m+[m[32m      eq('severity', row.severity, 'warning');[m
[32m+[m[32m      eq('type', row.type, 'note.preselect_failed');[m
[32m+[m[32m      eq('payload.approvedInterventionsEmpty', p.approvedInterventionsEmpty, true);[m
[32m+[m[32m      eq('payload.approvedMethodSetEmpty', p.approvedMethodSetEmpty, false);[m
[32m+[m[32m    },[m
[32m+[m[32m  },[m
[32m+[m[32m];[m
[32m+[m
[32m+[m[32mlet emitted = 0;[m
 let exitCode = 1;[m
 [m
 try {[m
   // ── Precondition: the table exists ─────────────────────────────────────────[m
   // Checked up front so an unapplied migration reports as itself. Without this the emitter's[m
[31m-  // pending-migration path would swallow the write and the run would fail with the far less useful[m
[31m-  // "row not found".[m
[32m+[m[32m  // pending-migration path would swallow every write and the run would fail with "row not found".[m
   const { rows: present } = await pool.query([m
     `select to_regclass('public.admin_alerts') is not null as present`,[m
   );[m
[36m@@ -100,79 +272,77 @@[m [mtry {[m
     );[m
   }[m
 [m
[31m-  // ── 1. Emit through the real code path ─────────────────────────────────────[m
[31m-  // type is the emitter's only defined AlertType key; reusing it (rather than inventing a[m
[31m-  // 'system.verification' key) keeps the union honest — nothing is added to the vocabulary just to[m
[31m-  // be tested. The row is identified by its payload, not its type.[m
   const { emitAdminAlert } = await import('@/lib/adminAlerts.ts');[m
 [m
[31m-  const before = Date.now();[m
[31m-  await emitAdminAlert({[m
[31m-    source: 'system',[m
[31m-    type: 'note.generation_failed',[m
[31m-    severity: 'critical',[m
[31m-    payload: { verification: true, ts },[m
[31m-  });[m
[31m-  const after = Date.now();[m
[31m-[m
[31m-  // ── 2. Read it back over the independent raw connection ────────────────────[m
[31m-  const { rows } = await pool.query([m
[31m-    `select id, created_at, source, type, severity, actor_user_id, client_id, payload, read_at[m
[31m-       from admin_alerts[m
[31m-      where payload->>'ts' = $1 and payload->>'verification' = 'true'`,[m
[31m-    [ts],[m
[31m-  );[m
[32m+[m[32m  for (const c of CASES) {[m
[32m+[m[32m    const ts = `${runId}:${c.label}`;[m
[32m+[m[32m    const before = Date.now();[m
[32m+[m[32m    // ── Emit through the real code path ──────────────────────────────────────[m
[32m+[m[32m    await emitAdminAlert({[m
[32m+[m[32m      ...c.alert,[m
[32m+[m[32m      payload: { ...c.alert.payload, verification: true, runId, ts },[m
[32m+[m[32m    });[m
[32m+[m[32m    emitted++;[m
[32m+[m[32m    const after = Date.now();[m
 [m
[31m-  if (rows.length === 0) {[m
[31m-    fail([m
[31m-      `no admin_alerts row was written for ts=${ts}. The emitter swallowed the write — ` +[m
[31m-        'check the server log for a [admin-alerts] line naming the cause.',[m
[32m+[m[32m    // ── Read back over the independent raw connection ────────────────────────[m
[32m+[m[32m    const { rows } = await pool.query([m
[32m+[m[32m      `select id, created_at, source, type, severity, actor_user_id, client_id, payload, read_at[m
[32m+[m[32m         from admin_alerts[m
[32m+[m[32m        where payload->>'runId' = $1 and payload->>'ts' = $2`,[m
[32m+[m[32m      [runId, ts],[m
     );[m
[31m-  }[m
[31m-  // More than one means the emitter wrote twice for a single call.[m
[31m-  if (rows.length > 1) {[m
[31m-    fail(`expected exactly 1 admin_alerts row for ts=${ts}, found ${rows.length} (duplicate write).`);[m
[31m-  }[m
 [m
[31m-  const row = rows[0];[m
[31m-  insertedId = row.id;[m
[31m-[m
[31m-  // ── 3. Assert every field, including the ones the emitter derives ───────────[m
[31m-  // All checks run before reporting so a mismatch shows the full picture rather than the first error.[m
[31m-  check('source', row.source, 'system');[m
[31m-  check('type', row.type, 'note.generation_failed');[m
[31m-  check('severity', row.severity, 'critical');[m
[31m-  // Not passed by this call — proves the emitter's `|| null` coercion, not a stray default.[m
[31m-  check('actor_user_id', row.actor_user_id, null);[m
[31m-  check('client_id', row.client_id, null);[m
[31m-  // NULL = unread; the panel's own state, never set at write time.[m
[31m-  check('read_at', row.read_at, null);[m
[31m-[m
[31m-  const payload = typeof row.payload === 'string' ? JSON.parse(row.payload) : row.payload;[m
[31m-  check('payload.verification', payload?.verification, true);[m
[31m-  check('payload.ts', payload?.ts, ts);[m
[31m-  const extraKeys = Object.keys(payload ?? {}).filter((k) => k !== 'verification' && k !== 'ts');[m
[31m-  if (extraKeys.length) failures.push(`payload has unexpected keys: ${extraKeys.join(', ')}`);[m
[31m-[m
[31m-  // created_at should come from the column default, i.e. land inside the call window (1s of slack[m
[31m-  // for clock skew between this process and the database server).[m
[31m-  const createdMs = new Date(row.created_at).getTime();[m
[31m-  if (!(createdMs >= before - 1000 && createdMs <= after + 1000)) {[m
[31m-    failures.push([m
[31m-      `created_at ${row.created_at} is outside the emit window ` +[m
[31m-        `${new Date(before).toISOString()}..${new Date(after).toISOString()}`,[m
[31m-    );[m
[32m+[m[32m    if (rows.length !== 1) {[m
[32m+[m[32m      // ONE emit must produce EXACTLY one row: 0 means the write was swallowed, >1 a duplicate write.[m
[32m+[m[32m      failures.push([m
[32m+[m[32m        `${c.label}: expected exactly 1 row, found ${rows.length}` +[m
[32m+[m[32m          (rows.length === 0 ? ' (write swallowed — check the server log for a [admin-alerts] line)' : ''),[m
[32m+[m[32m      );[m
[32m+[m[32m      continue;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    const row = rows[0];[m
[32m+[m[32m    const payload = typeof row.payload === 'string' ? JSON.parse(row.payload) : row.payload;[m
[32m+[m
[32m+[m[32m    // Shared invariants every alert must satisfy, whatever its type.[m
[32m+[m[32m    eq(`${c.label}: source`, row.source, c.alert.source);[m
[32m+[m[32m    eq(`${c.label}: type`, row.type, c.alert.type);[m
[32m+[m[32m    // Neither was passed — proves the emitter's `|| null` coercion rather than a stray default.[m
[32m+[m[32m    eq(`${c.label}: actor_user_id`, row.actor_user_id, null);[m
[32m+[m[32m    eq(`${c.label}: client_id`, row.client_id, null);[m
[32m+[m[32m    // NULL = unread; the panel's own state, never set at write time.[m
[32m+[m[32m    eq(`${c.label}: read_at`, row.read_at, null);[m
[32m+[m[32m    if (!/^[0-9a-f-]{36}$/i.test(String(row.id))) failures.push(`${c.label}: id is not a uuid: ${row.id}`);[m
[32m+[m[32m    // created_at should come from the column default, i.e. land inside the call window (1s of slack[m
[32m+[m[32m    // for clock skew between this process and the database server).[m
[32m+[m[32m    const createdMs = new Date(row.created_at).getTime();[m
[32m+[m[32m    if (!(createdMs >= before - 1000 && createdMs <= after + 1000)) {[m
[32m+[m[32m      failures.push(`${c.label}: created_at ${row.created_at} is outside the emit window`);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    // Per-case assertions. Labelled inside via the shared `failures` array.[m
[32m+[m[32m    const mark = failures.length;[m
[32m+[m[32m    c.expect(row, payload);[m
[32m+[m[32m    for (let i = mark; i < failures.length; i++) failures[i] = `${c.label}: ${failures[i]}`;[m
   }[m
[31m-  if (!/^[0-9a-f-]{36}$/i.test(String(row.id))) failures.push(`id is not a uuid: ${row.id}`);[m
 [m
   if (failures.length) {[m
[31m-    console.log('\nFAIL — the row was written but does not match what was sent:');[m
[32m+[m[32m    console.log('\nFAIL — the alert path did not behave as specified:');[m
     for (const f of failures) console.log(`  · ${f}`);[m
     exitCode = 1;[m
   } else {[m
[31m-    console.log(`\nPASS — emitAdminAlert wrote admin_alerts ${row.id} and every field matched.`);[m
[31m-    console.log(`  source=${row.source} type=${row.type} severity=${row.severity}`);[m
[31m-    console.log(`  actor_user_id=null client_id=null read_at=null payload.ts=${ts}`);[m
[32m+[m[32m    console.log(`\nPASS — ${CASES.length} alert types round-tripped, one row each, payloads intact.`);[m
[32m+[m[32m    for (const c of CASES) console.log(`  · ${c.label}`);[m
[32m+[m[32m    console.log([m
[32m+[m[32m      '\nNOT COVERED — that a real generation emits note.generated exactly once. That needs a live\n' +[m
[32m+[m[32m        'generation (real Azure call + real client), so verify it by hand: generate one note, then\n' +[m
[32m+[m[32m        "  select created_at, severity, payload->>'gateClean' as clean, payload->>'regenCount' as regens\n" +[m
[32m+[m[32m        "    from admin_alerts\n" +[m
[32m+[m[32m        "   where type = 'note.generated' and created_at > now() - interval '10 minutes'\n" +[m
[32m+[m[32m        '   order by created_at desc;\n' +[m
[32m+[m[32m        'Exactly one row per note you generated is the pass condition.',[m
[32m+[m[32m    );[m
     exitCode = 0;[m
   }[m
 } catch (e) {[m
[36m@@ -180,20 +350,30 @@[m [mtry {[m
   if (e?.stack) console.log(e.stack.split('\n').slice(1, 4).join('\n'));[m
   exitCode = 1;[m
 } finally {[m
[31m-  // ── 4. Remove exactly the row this run created ─────────────────────────────[m
[31m-  // In `finally` so a failed assertion never leaves a verification row behind. Deletes by id, so it[m
[31m-  // cannot touch anything else even if the query above somehow over-matched.[m
[31m-  if (insertedId) {[m
[32m+[m[32m  // ── Remove exactly the rows this run created ───────────────────────────────[m
[32m+[m[32m  // In `finally` so a failed assertion never leaves verification rows behind. Scoped to this run's[m
[32m+[m[32m  // id, so it cannot touch anything else — including rows from a concurrent run of this script.[m
[32m+[m[32m  if (emitted > 0) {[m
     try {[m
[31m-      const { rowCount } = await pool.query('delete from admin_alerts where id = $1', [insertedId]);[m
[31m-      if (rowCount === 1) {[m
[31m-        console.log(`  cleaned up: deleted admin_alerts ${insertedId}`);[m
[32m+[m[32m      const { rowCount } = await pool.query([m
[32m+[m[32m        `delete from admin_alerts where payload->>'verification' = 'true' and payload->>'runId' = $1`,[m
[32m+[m[32m        [runId],[m
[32m+[m[32m      );[m
[32m+[m[32m      if (rowCount === emitted) {[m
[32m+[m[32m        console.log(`\n  cleaned up: deleted ${rowCount} verification row(s) for run ${runId}`);[m
       } else {[m
[31m-        console.log(`  WARNING: cleanup deleted ${rowCount} rows for id ${insertedId} — remove it by hand.`);[m
[32m+[m[32m        console.log([m
[32m+[m[32m          `\n  WARNING: emitted ${emitted} row(s) but cleanup deleted ${rowCount}. ` +[m
[32m+[m[32m            `Remove leftovers by hand:\n` +[m
[32m+[m[32m            `    delete from admin_alerts where payload->>'runId' = '${runId}';`,[m
[32m+[m[32m        );[m
         exitCode = 1;[m
       }[m
     } catch (e) {[m
[31m-      console.log(`  WARNING: cleanup failed for id ${insertedId}: ${e?.message || e} — remove it by hand.`);[m
[32m+[m[32m      console.log([m
[32m+[m[32m        `\n  WARNING: cleanup failed (${e?.message || e}). Remove by hand:\n` +[m
[32m+[m[32m          `    delete from admin_alerts where payload->>'runId' = '${runId}';`,[m
[32m+[m[32m      );[m
       exitCode = 1;[m
     }[m
   }[m

                   SSUUMMMMAARRYY OOFF LLEESSSS CCOOMMMMAANNDDSS

      Commands marked with * may be preceded by a number, _N.
      Notes in parentheses indicate the behavior if _N is given.
      A key preceded by a caret indicates the Ctrl key; thus ^K is ctrl-K.

  h  H                 Display this help.
  q  :q  Q  :Q  ZZ     Exit.
 ---------------------------------------------------------------------------

                           MMOOVVIINNGG

  e  ^E  j  ^N  CR  *  Forward  one line   (or _N lines).
  y  ^Y  k  ^K  ^P  *  Backward one line   (or _N lines).
  f  ^F  ^V  SPACE  *  Forward  one window (or _N lines).
  b  ^B  ESC-v      *  Backward one window (or _N lines).
  z                 *  Forward  one window (and set window to _N).
  w                 *  Backward one window (and set window to _N).
  ESC-SPACE         *  Forward  one window, but don't stop at end-of-file.
  d  ^D             *  Forward  one half-window (and set half-window to _N).
  u  ^U             *  Backward one half-window (and set half-window to _N).
  ESC-)  RightArrow *  Right one half screen width (or _N positions).
  ESC-(  LeftArrow  *  Left  one half screen width (or _N positions).
  ESC-}  ^RightArrow   Right to last column displayed.
  ESC-{  ^LeftArrow    Left  to first column.
  F                    Forward forever; like "tail -f".
  ESC-F                Like F but stop when search pattern is found.
  r  ^R  ^L            Repaint screen.
  R                    Repaint screen, discarding buffered input.
        ---------------------------------------------------
        Default "window" is the screen height.
        Default "half-window" is half of the screen height.
 ---------------------------------------------------------------------------

                          SSEEAARRCCHHIINNGG

  /_p_a_t_t_e_r_n          *  Search forward for (_N-th) matching line.
  ?_p_a_t_t_e_r_n          *  Search backward for (_N-th) matching line.
  n                 *  Repeat previous search (for _N-th occurrence).
  N                 *  Repeat previous search in reverse direction.
  ESC-n             *  Repeat previous search, spanning files.
  ESC-N             *  Repeat previous search, reverse dir. & spanning files.
  ^O^N  ^On         *  Search forward for (_N-th) OSC8 hyperlink.
  ^O^P  ^Op         *  Search backward for (_N-th) OSC8 hyperlink.
  ^O^L  ^Ol            Jump to the currently selected OSC8 hyperlink.
  ESC-u                Undo (toggle) search highlighting.
  ESC-U                Clear search highlighting.
  &_p_a_t_t_e_r_n          *  Display only matching lines.
        ---------------------------------------------------
        A search pattern may begin with one or more of:
        ^N or !  Search for NON-matching lines.
        ^E or *  Search multiple files (pass thru END OF FILE).
        ^F or @  Start search at FIRST file (for /) or last file (for ?).
        ^K       Highlight matches, but don't move (KEEP position).
        ^R       Don't use REGULAR EXPRESSIONS.
        ^S _n     Search for match in _n-th parenthesized subpattern.
        ^W       WRAP search if no match found.
        ^L       Enter next character literally into pattern.
 ---------------------------------------------------------------------------

                           JJUUMMPPIINNGG

  g  <  ESC-<       *  Go to first line in file (or line _N).
  G  >  ESC->       *  Go to last line in file (or line _N).
  p  %              *  Go to beginning of file (or _N percent into file).
  t                 *  Go to the (_N-th) next tag.
  T                 *  Go to the (_N-th) previous tag.
  {  (  [           *  Find close bracket } ) ].
  }  )  ]           *  Find open bracket { ( [.
  ESC-^F _<_c_1_> _<_c_2_>  *  Find close bracket _<_c_2_>.
  ESC-^B _<_c_1_> _<_c_2_>  *  Find open bracket _<_c_1_>.
        ---------------------------------------------------
        Each "find close bracket" command goes forward to the close bracket 
          matching the (_N-th) open bracket in the top line.
        Each "find open bracket" command goes backward to the open bracket 
          matching the (_N-th) close bracket in the bottom line.

  m_<_l_e_t_t_e_r_>            Mark the current top line with <letter>.
  M_<_l_e_t_t_e_r_>            Mark the current bottom line with <letter>.
  '_<_l_e_t_t_e_r_>            Go to a previously marked position.
  ''                   Go to the previous position.
  ^X^X                 Same as '.
  ESC-m_<_l_e_t_t_e_r_>        Clear a mark.
        ---------------------------------------------------
        A mark is any upper-case or lower-case letter.
        Certain marks are predefined:
             ^  means  beginning of the file
             $  means  end of the file
 ---------------------------------------------------------------------------

                        CCHHAANNGGIINNGG FFIILLEESS

  :e [_f_i_l_e]            Examine a new file.
  ^X^V                 Same as :e.
  :n                *  Examine the (_N-th) next file from the command line.
  :p                *  Examine the (_N-th) previous file from the command line.
  :x                *  Examine the first (or _N-th) file from the command line.
  ^O^O                 Open the currently selected OSC8 hyperlink.
  :d                   Delete the current file from the command line list.
  =  ^G  :f            Print current file name.
 ---------------------------------------------------------------------------

                    MMIISSCCEELLLLAANNEEOOUUSS CCOOMMMMAANNDDSS

  -_<_f_l_a_g_>              Toggle a command line option [see OPTIONS below].
  --_<_n_a_m_e_>             Toggle a command line option, by name.
  __<_f_l_a_g_>              Display the setting of a command line option.
  ___<_n_a_m_e_>             Display the setting of an option, by name.
  +_c_m_d                 Execute the less cmd each time a new file is examined.

  !_c_o_m_m_a_n_d             Execute the shell command with $SHELL.
  #_c_o_m_m_a_n_d             Execute the shell command, expanded like a prompt.
  |XX_c_o_m_m_a_n_d            Pipe file between current pos & mark XX to shell command.
  s _f_i_l_e               Save input to a file.
  v                    Edit the current file with $VISUAL or $EDITOR.
  V                    Print version number of "less".
 ---------------------------------------------------------------------------

                           OOPPTTIIOONNSS

        Most options may be changed either on the command line,
        or from within less by using the - or -- command.
        Options may be given in one of two forms: either a single
        character preceded by a -, or a name preceded by --.

  -?  ........  --help
                  Display help (from command line).
  -a  ........  --search-skip-screen
                  Search skips current screen.
  -A  ........  --SEARCH-SKIP-SCREEN
                  Search starts just after target line.
  -b [_N]  ....  --buffers=[_N]
                  Number of buffers.
  -B  ........  --auto-buffers
                  Don't automatically allocate buffers for pipes.
  -c  ........  --clear-screen
                  Repaint by clearing rather than scrolling.
  -d  ........  --dumb
                  Dumb terminal.
  -D xx_c_o_l_o_r  .  --color=xx_c_o_l_o_r
                  Set screen colors.
  -e  -E  ....  --quit-at-eof  --QUIT-AT-EOF
                  Quit at end of file.
  -f  ........  --force
                  Force open non-regular files.
  -F  ........  --quit-if-one-screen
                  Quit if entire file fits on first screen.
  -g  ........  --hilite-search
                  Highlight only last match for searches.
  -G  ........  --HILITE-SEARCH
                  Don't highlight any matches for searches.
  -h [_N]  ....  --max-back-scroll=[_N]
                  Backward scroll limit.
  -i  ........  --ignore-case
                  Ignore case in searches that do not contain uppercase.
  -I  ........  --IGNORE-CASE
                  Ignore case in all searches.
  -j [_N]  ....  --jump-target=[_N]
                  Screen position of target lines.
  -J  ........  --status-column
                  Display a status column at left edge of screen.
  -k _f_i_l_e  ...  --lesskey-file=_f_i_l_e
                  Use a compiled lesskey file.
  -K  ........  --quit-on-intr
                  Exit less in response to ctrl-C.
  -L  ........  --no-lessopen
                  Ignore the LESSOPEN environment variable.
  -m  -M  ....  --long-prompt  --LONG-PROMPT
                  Set prompt style.
  -n .........  --line-numbers
                  Suppress line numbers in prompts and messages.
  -N .........  --LINE-NUMBERS
                  Display line number at start of each line.
  -o [_f_i_l_e] ..  --log-file=[_f_i_l_e]
                  Copy to log file (standard input only).
  -O [_f_i_l_e] ..  --LOG-FILE=[_f_i_l_e]
                  Copy to log file (unconditionally overwrite).
  -p _p_a_t_t_e_r_n .  --pattern=[_p_a_t_t_e_r_n]
                  Start at pattern (from command line).
  -P [_p_r_o_m_p_t]   --prompt=[_p_r_o_m_p_t]
                  Define new prompt.
  -q  -Q  ....  --quiet  --QUIET  --silent --SILENT
                  Quiet the terminal bell.
  -r  -R  ....  --raw-control-chars  --RAW-CONTROL-CHARS
                  Output "raw" control characters.
  -s  ........  --squeeze-blank-lines
                  Squeeze multiple blank lines.
  -S  ........  --chop-long-lines
                  Chop (truncate) long lines rather than wrapping.
  -t _t_a_g  ....  --tag=[_t_a_g]
                  Find a tag.
  -T [_t_a_g_s_f_i_l_e] --tag-file=[_t_a_g_s_f_i_l_e]
                  Use an alternate tags file.
  -u  -U  ....  --underline-special  --UNDERLINE-SPECIAL
                  Change handling of backspaces, tabs and carriage returns.
  -V  ........  --version
                  Display the version number of "less".
  -w  ........  --hilite-unread
                  Highlight first new line after forward-screen.
  -W  ........  --HILITE-UNREAD
                  Highlight first new line after any forward movement.
  -x [_N[,...]]  --tabs=[_N[,...]]
                  Set tab stops.
  -X  ........  --no-init
                  Don't use termcap init/deinit strings.
  -y [_N]  ....  --max-forw-scroll=[_N]
                  Forward scroll limit.
  -z [_N]  ....  --window=[_N]
                  Set size of window.
  -" [_c[_c]]  .  --quotes=[_c[_c]]
                  Set shell quote characters.
  -~  ........  --tilde
                  Don't display tildes after end of file.
  -# [_N]  ....  --shift=[_N]
                  Set horizontal scroll amount (0 = one half screen width).

                --exit-follow-on-close
                  Exit F command on a pipe when writer closes pipe.
                --file-size
                  Automatically determine the size of the input file.
                --follow-name
                  The F command changes files if the input file is renamed.
                --header=[_L[,_C[,_N]]]
                  Use _L lines (starting at line _N) and _C columns as headers.
                --incsearch
                  Search file as each pattern character is typed in.
                --intr=[_C]
                  Use _C instead of ^X to interrupt a read.
                --lesskey-context=_t_e_x_t
                  Use lesskey source file contents.
                --lesskey-src=_f_i_l_e
                  Use a lesskey source file.
                --line-num-width=[_N]
                  Set the width of the -N line number field to _N characters.
                --match-shift=[_N]
                  Show at least _N characters to the left of a search match.
                --modelines=[_N]
                  Read _N lines from the input file and look for vim modelines.
                --mouse
                  Enable mouse input.
                --no-keypad
                  Don't send termcap keypad init/deinit strings.
                --no-histdups
                  Remove duplicates from command history.
                --no-number-headers
                  Don't give line numbers to header lines.
                --no-search-header-lines
                  Searches do not include header lines.
                --no-search-header-columns
                  Searches do not include header columns.
                --no-search-headers
                  Searches do not include header lines or columns.
                --no-vbell
                  Disable the terminal's visual bell.
                --redraw-on-quit
                  Redraw final screen when quitting.
                --rscroll=[_C]
                  Set the character used to mark truncated lines.
                --save-marks
                  Retain marks across invocations of less.
                --search-options=[EFKNRW-]
                  Set default options for every search.
                --show-preproc-errors
                  Display a message if preprocessor exits with an error status.
                --proc-backspace
                  Process backspaces for bold/underline.
                --PROC-BACKSPACE
                  Treat backspaces as control characters.
                --proc-return
                  Delete carriage returns before newline.
                --PROC-RETURN
                  Treat carriage returns as control characters.
                --proc-tab
                  Expand tabs to spaces.
                --PROC-TAB
                  Treat tabs as control characters.
                --status-col-width=[_N]
                  Set the width of the -J status column to _N characters.
                --status-line
                  Highlight or color the entire line containing a mark.
                --use-backslash
                  Subsequent options use backslash as escape char.
                --use-color
                  Enables colored text.
                --wheel-lines=[_N]
                  Each click of the mouse wheel moves _N lines.
                --wordwrap
                  Wrap lines at spaces.


 ---------------------------------------------------------------------------

                          LLIINNEE EEDDIITTIINNGG

        These keys can be used to edit text being entered 
        on the "command line" at the bottom of the screen.

 RightArrow ..................... ESC-l ... Move cursor right one character.
 LeftArrow ...................... ESC-h ... Move cursor left one character.
 ctrl-RightArrow  ESC-RightArrow  ESC-w ... Move cursor right one word.
 ctrl-LeftArrow   ESC-LeftArrow   ESC-b ... Move cursor left one word.
 HOME ........................... ESC-0 ... Move cursor to start of line.
 END ............................ ESC-$ ... Move cursor to end of line.
 BACKSPACE ................................ Delete char to left of cursor.
 DELETE ......................... ESC-x ... Delete char under cursor.
 ctrl-BACKSPACE   ESC-BACKSPACE ........... Delete word to left of cursor.
 ctrl-DELETE .... ESC-DELETE .... ESC-X ... Delete word under cursor.
 ctrl-U ......... ESC (MS-DOS only) ....... Delete entire line.
 UpArrow ........................ ESC-k ... Retrieve previous command line.
 DownArrow ...................... ESC-j ... Retrieve next command line.
 TAB ...................................... Complete filename & cycle.
 SHIFT-TAB ...................... ESC-TAB   Complete filename & reverse cycle.
 ctrl-L ................................... Complete filename, list all.
